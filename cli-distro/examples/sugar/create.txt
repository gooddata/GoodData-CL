# A simple example that creates a Sugar CRM data model
# see full command documentation at http://developer.gooddata.com/gooddata-cl/cli-commands.html

# CREATE A NEW PROJECT
#CreateProject(name="Sugar CRM");

# Store the project ID to file for possible other scripts
UseProject(fileName="examples/sugar/pid");

# CREATE THE ACCOUNT
#UseSugarCrm(configFile="examples/sugar/account.xml",host="trial.sugarondemand.com/fanodg1159", username="jim", password="jim", entity="Accounts", fields="id, id, name, website, industry, rating");
#Dump(csvFile="examples/sugar/account.csv");
#TransferData();

# CREATE THE USER
UseSugarCrm(configFile="examples/sugar/user.xml",host="trial.sugarondemand.com/fanodg1159", username="jim", password="jim", entity="Users", fields="user_name, user_name, user_name, last_name, email1, department, team_name");
#Dump(csvFile="examples/sugar/users.csv");
TransferData();


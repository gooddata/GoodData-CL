# This example demonstrates how to load zendesk data to GoodData
# Lines starting with "#" are comments and will not be executed


# Retrieve the project that has been saved by the first script
RetrieveProject(fileName="examples/zendesk/pid");

# Load the zendesk data
LoadCsv(csvDataFile="examples/zendesk/data.csv", configFile="examples/zendesk/config.xml");

# Transfer the zendesk data to the GoodData project
TransferData(incremental="false");
